<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech/student/management/repository/ApplicationRepository">

    <!-- 申請登録 -->
    <insert id="insert">
        INSERT INTO applications (
        students_courses_id,
        status,
        created_at,
        updated_at
        ) VALUES (
        #{studentsCoursesId},
        #{status},
        NOW(),
        NOW()
        )
    </insert>

    <!-- 申請ステータス更新 -->
    <update id="updateStatus">
        UPDATE applications
        SET
        status = #{status},
        updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- 申請ID検索 -->
    <select id="findById" resultType="raisetech.student.management.date.Application">
        SELECT
        id,
        students_courses_id,
        status,
        created_at,
        updated_at,
        deleted_at
        FROM applications
        WHERE id = #{id}
    </select>
    
</mapper>
